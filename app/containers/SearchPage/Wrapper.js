import styled from 'styled-components';

const Wrapper = styled.div`
  li.slide.selected {
    cursor: pointer;
  }

  .MuiButton-root {
    padding: 15px;
    margin: 17px;
  }

  .MuiFormControl-root {
    margin-left: 10px;
  }
`;

export default Wrapper;
