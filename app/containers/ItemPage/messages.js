/*
 * ItemPage Messages
 *
 * This contains all the text for the ItemPage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ItemPage';

export default defineMessages({
  back: {
    id: `${scope}.back`,
    defaultMessage: 'Back to search',
  },
});
